{
  "name": "ai-research-agent",
  "version": "1.0.0",
  "description": "Daily AI research agent for automation oracle",
  "main": "index.js",
  "scripts": {
    "deploy": "npm run package && npm run upload",
    "package": "zip -r function.zip . -x '*.git*' -x 'node_modules/aws-sdk/*'",
    "upload": "aws lambda update-function-code --function-name ai-research-agent --zip-file fileb://function.zip --region us-east-1"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.450.0",
    "@aws-sdk/client-ses": "^3.450.0",
    "@aws-sdk/lib-dynamodb": "^3.450.0",
    "openai": "^4.20.0",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {}
}
